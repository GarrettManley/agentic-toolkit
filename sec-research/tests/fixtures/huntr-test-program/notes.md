# Fixture: huntr-test

This is a synthetic program scope for end-to-end and unit testing. It includes
in-scope/out-of-scope assets that exercise the scope-match library, and a
manual-form submission protocol so e2e smoke can run without hitting a real venue.

The repo and package referenced here do NOT actually exist; tests should not
attempt to fetch them.
