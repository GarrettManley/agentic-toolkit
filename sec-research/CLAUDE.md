# sec-research/ — Claude Code workspace context

This file is auto-loaded by Claude Code when a session starts inside this directory. It is the canonical orientation document for Claude — read it before doing anything in this workspace.

## Purpose (one paragraph)

`sec-research/` is a hard-bounded, evidence-disciplined security-research workspace whose sole purpose is to produce **reproducible, Tier-1-citable findings** that pass human review before submission to bug bounty programs. Optimization target is rejection-resistance against "AI-Slop" anti-patterns. Anchor venues: huntr.com, GitHub Security Advisories, IBB on HackerOne, direct upstream maintainer disclosure. Anchor target class for v1: OSS supply-chain and dependency vulnerabilities (npm / PyPI / cargo / rubygems CVE-style findings).

## Three invariants (load-bearing — every hook enforces one)

1. **Scope-bounded** — Every network call, file write outside this dir, and target identifier MUST trace back to a loaded `programs/<slug>/scope.yaml`. No exceptions without a signed override token.
2. **Evidence-grounded** — No claim ships to a finding draft without (a) a `Citation:` from a Tier-1 source, (b) a runnable PoC at `findings/<trace>/poc/reproduce.sh`, AND (c) captured execution evidence in `findings/<trace>/evidence/`.
3. **Human-gated** — Every submission requires a human-signed approval token. Drafts can be auto-generated; sends cannot. PT-2 (the submission gate) has **no override path**.

## Quick commands (copy-paste ready)

```powershell
# Health check — run first if anything seems off
python scripts/init_workspace.py --verify

# Load a program scope (REQUIRED before any recon — unblocks PT-1)
python scripts/load_program.py --venue ghsa --identifier <repo-slug>

# Pre-submission flow (sign_approval requires interactive human typing)
python scripts/verify_finding.py <trace-id>
python scripts/sign_approval.py <trace-id> --venue <venue>
python scripts/submit.py --trace <trace-id> --token <token-id>

# Generate a signed override (interactive; you cannot self-issue)
python scripts/sign_override.py --rule PT-X --target "<target>"

# Query the ledger
python scripts/ledger_query.py --list

# Run hook integration tests
pytest
```

## Behavioral rules for Claude (read carefully)

These are protective — they prevent you from triggering hooks that would block your work mid-stream, and they prevent the workspace from producing rejectable findings.

- **Pre-empt scope checks.** Before any reconnaissance, HTTP fetch, or scripted lookup against an external host, confirm a program scope is loaded (`ls programs/`). If none is loaded and the user is asking for recon work, propose loading one with `python scripts/load_program.py --venue <ghsa|huntr|...> --identifier <slug>` and wait for confirmation — don't attempt the call and let PT-1 block.
- **Don't fabricate references.** Every CVE ID, package@version, and commit SHA you write into `findings/**/*.md` is independently verified by PT-4 against NVD / npm / PyPI / cargo / rubygems / GitHub. If you don't have a verified reference, say "unverified" rather than inventing one. PT-4 will catch fabrications either way; saying "unverified" is faster.
- **Every Fact: line has Citation: + Proof:.** When writing `finding.md`, the discipline is non-negotiable. If you write a `Fact:` or `Claim:` line, the next ~12 lines MUST contain `Citation:` (URL to a Tier-1 or Tier-2 source) and `Proof:` (code excerpt, command output, or HTTP trace). PoT-2 enforces this.
- **PoC code is gated by PT-5.** PT-5 blocks direct `npm install` / `pip install` / `cargo install` / `bash poc/reproduce.sh` outside a sandbox wrapper. **Stage-1 caveat:** `sandbox_server.py` is not built yet (Stage 4); `verify_finding.py` currently invokes PoCs directly, and PT-5's bypass triggers on the literal substring `sandbox_server` or `docker`. Don't fabricate a sandbox call you can't actually run — if you genuinely need to execute a PoC pre-Stage-4, request a signed PT-5 override.
- **Never call submit.py without a valid `--token`.** The token is generated by `scripts/sign_approval.py`, which requires an interactive human-typed Trace-ID confirmation. PT-2 has **no override** — there is no path that bypasses human approval for a submission.
- **Trace-IDs are `FIND-YYYY-MM-DD-NNN`.** Use today's date in UTC. Sequence number is per-day, zero-padded to 3 digits. G-4 enforces this in commit messages when `findings/` files are staged.
- **Citation tiers.** Tier 1 = primary source (the source code itself, official advisory, package manifest, NVD record). Tier 2 = corroborating secondary (a maintainer's blog post, a vendor advisory). Tier 3 = tertiary (third-party blog, news article) — **requires a `corroborator_url` field**. Schema enforces.
- **Status enum.** Findings progress: `draft-incomplete → draft-complete → verifying → ready-for-review → approved-to-submit → submitted → {accepted|rejected|duplicate|informational}`. `sign_approval.py` only operates on `ready-for-review` findings.

## Hook map (the 17 rules — minimal table)

| Rule | Event | Blocks if... | Override |
|------|-------|--------------|----------|
| PT-1 | PreToolUse / network | target host not in any loaded scope | signed |
| PT-2 | PreToolUse / submit | no `--token` or invalid token | **NONE** |
| PT-3 | PreToolUse / Edit-Write | file_path outside `sec-research/` | signed |
| PT-4 | PreToolUse / Edit-Write to findings/**.md | unverifiable CVE/pkg/SHA | signed |
| PT-5 | PreToolUse / Bash | runs PoC or registry install outside sandbox | signed |
| PT-6 | PreToolUse / Edit-Write to evidence/redacted/ | secret detected | **NONE** |
| PoT-1 | PostToolUse / new findings/<dir> | (auto-correct: log missing trace_id) | n/a |
| PoT-2 | PostToolUse / finding.md | Fact/Claim lacks Citation+Proof | signed |
| PoT-3 | PostToolUse / Bash in finding ctx | timeline.md write failure | signed |
| S-1 | Stop | session-modified finding incomplete & not draft-incomplete | signed |
| S-2 | Stop | session log can't be written | signed |
| UPS-1 | UserPromptSubmit | testing prompt + no scope (injects reminder) | n/a |
| UPS-2 | UserPromptSubmit | named target not in loaded scope | signed |
| G-1 | git pre-commit | findings/** fails verify_finding.py | `--allow-incomplete` + signed |
| G-2 | git pre-commit | secret in staged content | **NONE** |
| G-3 | git pre-push | G-1+G-2 fail on unpushed commits | (inner rule) |
| G-4 | git commit-msg | findings/ staged but message lacks `Trace-ID:` | signed |

Full contract definitions live in `docs/HOOK_CONTRACTS.md`.

## Override mechanism (signed HMAC tokens)

- Issuance: `python scripts/sign_override.py` (interactive; key at `~/.claude/sec-research-override-key`, OUTSIDE this repo).
- Format: HMAC-SHA256 over canonical JSON.
- Single-use by default. Max TTL 4h. Hard ceilings: max_uses ≤ 5, expires_at ≤ now + 24h.
- Used tokens move to `overrides/used/` (gitignored).
- **You (Claude) cannot self-issue overrides.** The HMAC key is intentionally outside the repo so that nothing inside the loop can sign tokens. Always direct the user to run `sign_override.py` manually.

## Where to look when you need depth

- `docs/CHARTER.md` — full charter, invariants, roadmap (Stages 1-7).
- `docs/HOOK_CONTRACTS.md` — every hook's trigger, check, fail mode, override path.
- `docs/SCOPE_SCHEMA.md` — how program scopes are normalized.
- `docs/EVIDENCE_DISCIPLINE.md` — required evidence per vuln class.
- `docs/SUBMISSION_GATE.md` — the verify → sign-approval → submit flow.
- `docs/CREDENTIAL_HANDLING.md` — keyring / Windows Credential Manager usage.
- `docs/PLAYBOOK_FORMAT.md` — playbook schema for the self-improvement substrate.
- `docs/INDEX.md` — append-only Trace-ID registry across all findings.
- `schema/*.json` — JSON Schema 2020-12 contracts for program, finding, evidence, override, submission.
- `hooks/{pretooluse,posttooluse,stop,userpromptsubmit}.py` — hook implementations (rule IDs map 1:1 to functions).
- `tests/` — pytest suite covering hook behavior, schemas, and e2e smoke. Run: `pytest`.
- `runtime/` — **gitignored**: sandbox output, raw recon, briefings, caches, session logs, scheduled-runs.jsonl.
- `submissions/ledger.jsonl` — append-only audit log of every submission/override event.
- `README.md` — human-facing quickstart (this CLAUDE.md is the Claude-facing version).

## Standalone mode

This workspace is configured to be opened **directly with `claude` started inside `sec-research/`**. Hook paths in `.claude/settings.json` resolve via `${CLAUDE_PROJECT_DIR}`, which when launched here points to this directory. **Opening Claude in the parent workspace will NOT load these hooks** — the `.claude/settings.json` is only discovered when launched here.

Git is still nested under the parent's `.git`, so `git rev-parse --show-toplevel` returns the parent. The `scripts/git-hooks/*` files compensate by joining `sec-research/` to that path. If this workspace is ever extracted to a sibling repo, those hook scripts and `install_git_hooks.ps1` need to drop the `sec-research/` segment.

## Stage 1 vs Stages 2-7

Currently Stage 1 of 7 (foundation/governance). Recon (Stage 3), hypothesis generation (Stage 4), and venue-specific submission integrations beyond GHSA + manual-form (Stage 7) are NOT yet implemented — `nightly.py` and `investigate.py` are pipeline skeletons that exercise hooks against fixture data. Don't claim a finding can be auto-discovered yet; the framework is ready, the discovery logic is not.
